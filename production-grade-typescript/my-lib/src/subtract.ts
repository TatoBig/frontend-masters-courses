/**
 * Calculate the diference between two numbers
 * 
 * @param a - first number
 * @param b - second numer
 * 
 * @beta
 */

export function subtract(a: number, b: number): number {
  return a - b;
}
